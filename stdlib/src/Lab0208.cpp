#include "bel.hpp"

карыстаць вобласць std;

вобласць беларусь
{
	клас лаб0208
	{
	публіч:
		стат цэл фактарыял(цэл лічба)
		{
			вярнуць(лічба >= 1) ? лічба *фактарыял(лічба - 1) : 1;
		}

		стат пуст запуск()
		{
			цэл лічба;
			вывад << "Enter the number: ";
			ўвод >> лічба;

			цэл ф = фактарыял(лічба);
			вывад << ф << абзац;
		}
	};
}
